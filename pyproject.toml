[project]
name = "alpha-options-agent"
version = "0.1.0"
description = "Autonomous options-trading agent on Alpaca paper trading (lablab.ai x Alpaca hackathon)"
requires-python = ">=3.11"
dependencies = [
    "alpaca-py>=0.33.0",
    "python-dotenv>=1.0.1",
]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
